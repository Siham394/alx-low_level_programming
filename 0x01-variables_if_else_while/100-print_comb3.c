#include <stdio.h>

/**
 * main - all possible combination of two digits
 * Return: Always 0
 */
int main(void)
{
	int a,b;
	a=1;
	b=1;
	if (a = b)
		putchar((a % 10) + '0');
	else if (a < b)
		putchar((b % 10) + 'o');
	else if(a > b)
		putchar((a % 10) + 'o');
	a++;
		b++;
}
